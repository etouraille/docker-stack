#!/bin/bash

export ENV=dev
docker-compose kill
sudo chown -R root:root php/ini
sudo chown -R root:root php/phpConf
sudo chown -R root:root php/ini
sudo chown -R root:root php/ini
docker-compose up -d

